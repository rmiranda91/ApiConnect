@{
    Layout = null;
}

@model CentroMedicoQuirurgico.Models.Entity.ValidateUserRequest
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Ingreso</title>

    @Styles.Render("~/Content/css")

</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                <br />
            </div>
        </div>
        <div class="row justify-content-center align-items-center loginBox">
            <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
                <div class="card border-primary mb-3">
                    <div class="card-header text-center"><h3><b>Ingreso</b></h3></div>
                    <div class="card-body">
                        @using (Html.BeginForm())
                        {
                            
                            <div class="form-group">
                                <label for="txt_usuario"><b>Usuario</b></label>
                                @Html.TextBoxFor(m => Model.user, new { @class="form-control", @placeholder="Ingresa tu usuario" })
                            </div>
                            <div class="form-group">
                                <label for="exampleInputPassword1"><b>Contrase&ntilde;a</b></label>
                                @Html.PasswordFor(m => Model.password, new { @class="form-control", @placeholder="Ingresa tu contraseña" })
                            </div>
                            <div class="form-group">
                                @ViewBag.message
                            </div>
                            
                            <button type="submit" class="btn btn-primary btn-block">Iniciar sesi&oacute;n</button>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

    @Scripts.Render("~/bundles/jquery", "~/bundles/bootstrap")
</body>
</html>
